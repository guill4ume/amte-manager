export interface DolCharacter {
	AccountName: string,
	Name: string,
	GuildID: string,
	GuildRank: number,
	Level: number

	// Way more available in db but I'm not masochist enough to pasta everything
}
